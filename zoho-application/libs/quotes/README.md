# quotes-lib

This library is used fore every thing related to quotes-lib in Zoho.

currently it has the following features:

- create a quote agency pdf
- create a quote client flyer
- prepare a quote for pdf generation

## Running unit tests

Run `nx test quotes-lib` to execute the unit tests via [Jest](https://jestjs.io).
